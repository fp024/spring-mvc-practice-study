# Bootstrap 5 스타일 클래스로 전환해야할 이전 스타일 클래스


### `font-weight-bold` -> "fw-bold"
`font-weight-bold`는 Bootstrap 4에서 사용하던 클래스.
Bootstrap 5에서는 다음처럼 사용함.

```html
<h6 class="fw-bold">제목</h6>
```

- `fw-bold`: Bootstrap 5 제공
- `font-weight-bold`: Bootstrap 4 스타일
